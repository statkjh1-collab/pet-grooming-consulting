# data.go.kr 공공데이터포털에서 발급받은 서비스키 (Decoding/일반 인증키)
# "상가업소정보" API 활용신청 후 발급 (승인까지 1~2시간 소요)
DATA_GO_KR_SERVICE_KEY=

# Render Postgres 연결 문자열. 비워두면 로컬 SQLite(backend/startup.db)로 폴백.
# 로컬에서는 External Database URL을, Render 웹서비스 자체는 Internal Database URL을 사용.
DATABASE_URL=
